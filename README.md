# Era Dominion 3D v23 Single File

이 버전은 업로드 오류를 줄이기 위해 `assets` 폴더 없이 `index.html` 하나로 실행되도록 만든 버전입니다.

GitHub에는 압축을 푼 뒤 `index.html`과 `README.md`만 올리면 됩니다.
기존 저장소에 남아 있는 예전 `assets` 폴더가 있어도 이 버전은 영향을 받지 않습니다.
